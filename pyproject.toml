[project]
name = "nhp-capacity-conversion-logic"
version = "0.1.0"
description = "Repository for converting hospital activity into capacity estimates"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "azure-core>=1.38.0",
    "azure-data-tables>=12.7.0",
    "azure-identity>=1.25.1",
    "azure-storage-blob>=12.28.0",
    "dotenv>=0.9.9",
    "nhp-products",
    "openpyxl>=3.1.5",
    "pandas>=3.0.0",
    "pandas-stubs>=3.0.0.260204",
]

[build-system]
requires = ["setuptools>=80", "setuptools-scm>=8", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.uv.sources]
nhp-products = { git = "https://github.com/the-strategy-unit/nhp_products.git", rev = "v4.4.0" }

[tool.uv.pip]
# Connect must use the commit-pinned URLs exported from uv.lock, not transitive source overrides.
no-sources = true

[dependency-groups]
app = [
    "shiny[theme]>=1.7.0,<2", # 1.7.0 passed the Connect deployment smoke test; review before Shiny 2.
]
dev = [
    "pandas-stubs>=3.0.0.260204",
    "pyright>=1.1.408",
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
    "pytest-mock>=3.15.1",
    "pytest-playwright>=0.8.0",
    "rsconnect-python>=1.30.0,<2",
    "ruff>=0.15.0",
    "ty>=0.0.19",
]

[tool.pyright]
ignore = [      
    "build",
]

[tool.coverage.run]
source = ["src/nhp"]
omit=["tests/*"]

[tool.coverage.report]
exclude_also = ['if __name__ == .__main__.:']
skip_empty = false
show_missing = true

[tool.pytest.ini_options]
addopts = "-vv"
testpaths = ["tests"]
